version: 2.1
jobs:
  fake_smoke_test:
    docker:
      - image: circleci/node:13.8.0
    steps:
      - checkout
      - run:
          name: Run Fake Smoke Test
          command: if curl -s --head "https://google.com" 
                   then
                     echo "It worked!"
                   else
                     echo "It failed"
                   fi
workflows:
  my_workflow:
    jobs:
      - fake_smoke_test
